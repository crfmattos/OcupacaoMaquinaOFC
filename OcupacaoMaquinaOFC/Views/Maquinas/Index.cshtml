@model IEnumerable<OcupacaoMaquinaOFC.Models.Maquina>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.limiteHoras)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.valorMaquina)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.limiteHoras)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.valorMaquina)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.nome">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.nome">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.nome">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
